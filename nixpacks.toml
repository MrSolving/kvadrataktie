[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = [
  "cd server && npm ci",
  "cd client && npm ci"
]

[phases.build]
cmds = ["cd client && npm run build"]

[start]
cmd = "TZ=Europe/Stockholm NODE_ENV=production node server/src/index.js"
